import {createRoot} from 'react-dom/client';
import App from './App';

createRoot(document.getElementById('root')).render(<App/>);  //create a new "root" for our React application.  "root" refers to the top-level container that holds all of the other components in the application